import { FlatList, StyleSheet, View, Text} from 'react-native';
import ListItem  from '../../components/ListItem'; // Assurez-vous que ListItem est bien importé

export default function App() {

  return (
    <ListItem />
  );
}

const styles = StyleSheet.create({});
